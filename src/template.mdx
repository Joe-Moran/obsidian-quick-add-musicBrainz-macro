import templateValues from './data/obsidianTemplateAlbumValues';

---

title: { `{{VALUE:${templateValues.TITLE}}}`}

artist: { `{{VALUE:${templateValues.ARTIST}}}` }

rating: { `{{VALUE:${templateValues.RATING}}}` }

date: { `{{VALUE:${templateValues.RELEASE_DATE}}}` }

formats: { `{{VALUE:${templateValues.OWNDED_FORMATS}}}` }

genres: { `{{VALUE:${templateValues.GENRES}}}` }

id: { `{{VALUE:${templateValues.ID}}}` }

cover: "![img] { `({{VALUE:${templateValues.COVER_PATH}}})` }"

---

`$= dv.current().cover`
